# 2026-08-01 工作日志

## 服务运维
- 重启 Flask 服务于 8080（DEMO_MODE=on，系统 Python 3.14.3）。首页健康检查 HTTP 200 通过。
- 修复项回顾：`templates/alert.html` 的 `onclick="loadAlerts()"` 调用未挂 window 的内部函数 → `static/js/app.js` 已 `window.loadAlerts = loadAlerts`（7/31 修，今日确认生效）。

## AI 智能助手（Agentic）功能进展（重大）
- **PRD**：`docs/ai-agent-prd.md`（许清楚）— 7 个工具、意图理解 + ReAct、危险确认、会话持久化。
- **架构设计**：`docs/ai-agent-design.md`（高见远）— ReAct prompt、工具注册表、会话 schema、T01-T10 任务分解。
- **后端 MVP 已实现并验证**：`core/ai_agent/{__init__,agent,executor,session,tools}.py` + `api/ai_agent.py`（挂 api_bp，非独立蓝图）。
  - 工具：run_backup_task / run_inspection / list_recent_records / list_alert_predictions / get_storage_usage / list_tasks / get_inspection_report（7 个，对应现有 API）。
  - 端点：`POST /api/agent/chat`、`POST /api/agent/confirm`、`POST|GET|DELETE /api/agent/sessions`、`GET /api/agent/sessions/<id>/messages`。
  - 危险工具（run_backup_task、run_inspection）requires_confirm=True → 首次 chat 返回 pending_confirm，前端后续调 confirm。
  - 复用 `core/ai_alert.py` 的 `AIPredictor._call_model` / `_parse_response`。
- **测试**：`tests/test_ai_agent.py` 105 passed（3.3s）。
- ⚠️ 之前工程师被 codebuddy 平台 429（额度限流）误判"未实现"，实际代码已落盘且测试通过。

## 待办（未做）
- **前端 UI（T07-T10）已补齐**（8/1 完成）：`templates/agent.html`（5655B，两栏布局+确认弹窗）、`static/css/agent.css`（9114B，71 条样式）。聊天逻辑早已在 `static/js/app.js` 行 4243-4682 实现（`initAgent`/`sendAgentMessage`/`submitAgentConfirm` 等），仅缺 HTML 模板与 CSS。模板渲染验证 STATUS 200，19 个 DOM id 与 46 个 class 全对齐。用户 Ctrl+F5 刷新即可使用。
- AI 模型 provider=local 仍仅校验路径，未实现真实本地推理（标注"本地(未实现)"）。
- RT 备份 T02-T05 待续（T01 已完成，33 测试过）。

## 注意
- API 蓝图统一用 `api/__init__.py` 的 `api_bp`，勿建独立 Blueprint。
- 系统 Python 3.14.3 有依赖；managed 3.13.12 无依赖。

## UX 反馈增量闭环（下午，5 轮 SOP）
- 4 项体验问题（AI 告警明细+数据验证 / 实时备份任务选择 / 恢复页去鼎甲文案+卡片 / 容灾 HA 与数据同步整合）经 PM 增量 PRD → 架构师设计(T01-T10) → 工程师实现 → QA 两轮验收，最终**完全通过**：340 passed/3 skipped，0 遗留。
- 设计/验收文档：`docs/ux-feedback-20260801-{prd,design,verification}.md`。
- QA 第1轮发现 B1(P0, /sources 前后端契约不一致) + G1(P2, 实时创建未检测守护态)，转工程师修复后第2轮全闭环。
- 改后端模块后必须重启 8080 服务（DEMO_MODE=on，系统 Python）才生效。

## RT 备份 T02-T05 收口（晚间）
- **重大核查结论**：用户以为"T02-T05 未做"，实际代码与测试早已全部落地（此前记忆笔记已过时）。真相：T01-T05 全实现，仅 **调度生命周期集成（T03-S3）** 是真实缺口 —— `core/scheduler.py` 原本完全没接入 RtSupervisor，导致服务重启后 RT 守护不会自动拉起。
- 派工程师寇豆码补 `core/scheduler.py`：`_register_rt_backup()` 在 `start_scheduler()` 拉起 `RtSupervisor.start(sched)`（复用共享 APScheduler 实例，避免 gunicorn -w N 每进程各起调度器）+ 注册 health(1min)/prune(1h)/watchdog(5min) 三周期 job（均 max_instances=1+coalesce=True）；`stop_scheduler()` 优雅停守护；`reload_scheduler()` 跳过 rt_supervisor_tick。门面 `start(scheduler=None)` 透传。新增 `tests/test_rt_scheduler.py`（5 用例）。
- QA 严过关复验：调度集成 E2E 5/5 PASSED；全量 `pytest tests/` **347 passed / 1 skipped**。并修复环境缺口：活动 Python(3.13.12) 缺 apscheduler → 装 APScheduler 3.11.3 后转绿（线上系统 Python 3.14.3 本就有，无影响）。
- 遗留建议（非阻塞）：`core/rt_backup/{health,repo,types,pitr}.py` 与 `watchers/` 尚无对应单测文件，建议后续补。
- 平台坑：派发给子代理的初始任务消息内容偶发为空（undefined），但子代理能依据团队目标正确推断范围并交付，结果无误。

## 服务启动方式修正（关键，16:09）
- **发现长期错误**：此前所有"服务起起来"都用 `python app.py`，但 `app.py` 只 `create_app()` 不启动调度器，导致定时备份/巡检/RT 守护全没在跑（掩盖了本应收口的 RT 自动拉起）。正确入口是 **`run.py`**（`run.py:19` 调 `scheduler.start_scheduler()` + `app.run`）。
- 已用 `DEMO_MODE=on python run.py` 重启（新 PID 7584）：日志确认 `[rt.supervisor] 已启动，管理 0 个实时任务` + `已注册周期任务 rt_health / rt_prune / rt_watchdog` + `调度器已启动，已注册 9 个任务`，`/alert` 探测 302。**RT 守护随服务自动拉起，收口价值真正兑现**。
- 注意点：之前 app.py 起的进程 PID 17336 用 `taskkill //PID`（Git Bash 路径转换报错）杀不掉，改用 PowerShell 工具 `Stop-Process -Id 17336 -Force` 成功释放 8080。Bash 内嵌 powershell 命令被安全策略拦截，须用独立 PowerShell 工具。

## 记录展示统一与搜索 收口（16:39）
- **背景**：7/31 用户据 3 张截图要求备份/恢复记录统一格式「业务系统+IP+备份类型+备份方式+备份时间」并加按业务系统/IP 搜索；数据恢复页下拉同样格式（缺备份方式）。用户拍板：业务系统=任务名+保留 `#id`，类型/方式汉化（full→全量、incremental→增量、file→文件）。
- **实现（主理人直接接手，因架构师/通用子代理框架故障 "Cannot read properties of undefined"）**：`config.py` 补 `file` 键+`BACKUP_TYPE_DISPLAY_NAMES`；`core/models.py` 新增 `normalize_host_ip`(R1规则，遇"本地"不吞)、`list_records`/`list_restores` 加 `keyword`+`LEFT JOIN backup_tasks`+五要素填充；`api/records.py`/`api/restore.py` 透传 keyword；`static/js/app.js` 新增 `fmtRecordLabel`/`fmtBizCell` 并改造 6 入口；`templates/{records,restore_records,restore}.html` 加搜索框与五要素表头。
- **验证**：py_compile 通过；`tests/test_record_display.py` 12 passed（归一化4形态+配置映射+list_records/restores 五要素+keyword）；全量 `pytest tests/` **357 passed / 3 skipped**；线上 API 实测 `/api/records` 89 条含五要素、`keyword=本地` 搜出 9 条且 `host_ip:"本地"` 保留无误、`/api/restores` 12 条五要素正确。
- **服务重启修复 stale RT 锁**：run.py 被强杀后残留 `instance/rt_supervisor.lock` 致新进程 `[rt.supervisor] 未抢到锁` 不启动 RT 守护。已 `rm -f instance/rt_supervisor.lock` + 重启，日志确认 `[rt.supervisor] 已启动，管理 0 个实时任务` + `调度器已注册 9 个任务`（较此前 5 个新增 RT 三周期 job）。
- 文档：`docs/record-display-prd.md`(许清楚)、`docs/record-display-design.md`(新增，主理人归档)。
