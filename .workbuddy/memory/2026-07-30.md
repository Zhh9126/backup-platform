# 2026-07-30 工作记录

## 银行级三位一体架构优化（Phase 0/1/2）
基于用户提供的银行级"三位一体"备份容灾蓝图（分层分级保护 / 统一管控平台 / 运维保障体系），对平台做架构优化。先由架构师产出 `docs/architecture_optimization_design.md`（总体架构分层图 + 能力盘点 + GAP 表 G1-G13 + 5 个 Phase 路线 + 领域模型）。用户拍板范围：**Phase 0+1+2 一起做**，信创核心库走自研适配器轨道，AI 告警用规则+轻量统计，磁带 L4 暂不落地。

### 交付内容
- **Phase 0**：`protection_policies` 表 + backup_tasks 派生列（policy_id/protection_level/adapter_tier/rpo/rto）；`core/policy.py` ProtectionPolicyService（按 level 解析默认 RPO/RTO/备份策略）；`ENGINE_REGISTRY` 加 adapter_tier（oracle/kingbase/dameng=core_self，其余=peripheral_api）；`AdapterContract` 契约；`api/policy.py` 挂 api_bp；`templates/protection.html` + 导航 + initProtection()。
- **Phase 1**：`backup_sets` 表（full/incremental/synthetic_full 增量链）；`synthesize_full()` 合成全量（mysql 物理 xtrabackup 合并 + 逻辑占位 + DEMO 兜底）；scheduler 并发线程池 + 带宽令牌桶 + 避峰窗口（用 ProtectionPolicyService.resolve 取并行度）；存储后端对象级去重（sha256 索引 + dedup_saved_bytes）+ 分块上传；`core/lifecycle.py` LifecycleEngine（L1→L2→L3 按龄/量流转/到期，复用 tier_replication 底层）；`api/lifecycle.py` + 周期 job；storage.html 生命周期卡片。
- **Phase 2**：`migration_plans/clone_requests/hetero_jobs/itsm_tickets` 表；`core/migration.py`（pre 黄金点+verify / mid 高频增量 / post 重心切换+保留期）；`core/clone_service.py`（申请→ITSM审批→拉VDB→到期销毁状态机）；`core/itsm.py`（InternalITSMAdapter + 钉钉/ServiceNow stub 可插拔）；`core/hetero_convert.py`（Oracle→分布式备份集转换，DEMO 兜底）；`api/migration.py/clone.py/itsm.py`；`templates/migration.html/clone.html` + 导航 + initMigration/initClone；scheduler 注册 clone 到期 job。

### 关键坑
- 运行必须用系统 Python 3.14.3（C:\Users\zhouhuanhuan\AppData\Local\Microsoft\WindowsApps\python.exe），默认 `python` 3.13 缺 APScheduler。
- QA 第 1 轮发现 core/itsm.py 源码 Bug：`_ExternalITSMStub.__init__(self, system)` 强制参数，工厂无参实例化 DingTalk/ServiceNow 子类崩 TypeError。修复：给两子类补 `def __init__(self): super().__init__(self.system)`。

### 验证
- 工程师自测：Phase0 9/9、Phase1 20/20、Phase2 21/21 均 IS_PASS: YES。
- QA 二线独立回归 `tests/qa_phase_0_1_2.py`：第1轮 43/44（itsm Bug），修复后第2轮 **44/44 全绿**，零遗留。
- 运行：`set DEMO_MODE=on && 系统Python run.py`，访问 http://localhost:8080（admin/admin123）→ 备份管理>保护策略 / 存储管理>生命周期 / 运维>迁移保护·克隆服务。

## 后续可选（用户未要求，未做）
Phase 3（容灾链路HA/日志填补/一致性校验 + AI预测告警）、Phase 4（演练制度化季度排程+趋势 + 备份数据价值挖掘）、磁带 L4、ITSM 真实对接（钉钉/ServiceNow 凭证）、元数据 SQLite→PostgreSQL 演进。

---

## Phase 3+4 已完成（同日续做）
用户确认"Phase 3+4 一起干"，并先修了 /migration /clone 的 404（根因：3 个旧进程抢占 8080，旧代码无这两个路由）。
- **Phase 3**（IS_PASS YES，自检 25/25）：`disaster_links` + `alert_predictions` 表；`core/disaster_link.py` DisasterLinkEngine（多专线智能选路 select_route / 日志间隙填补 fill_log_gap / 备端一致性校验 run_consistency_check 总分核对+抽样校验和）；`core/ai_alert.py` AIPredictor（backup_fail/storage_full/link_degraded/drill_overdue/rpo_breach 五类风险，规则+轻量统计，critical 自动通知，predict_with_model ML 插桩点）；`api/link.py` + `api/ai_alert.py` 挂 api_bp；`templates/drlink.html`/`alert.html` + 导航 + initDrLink/initAlert；scheduler 注册 ai_alert 周期任务（默认 6h）。
- **Phase 4**（IS_PASS YES，自检 30/30）：`core/drill.py` 增强（run_scheduled_drill 季度排程 / get_trend RTO-RPO 趋势含负值清洗 / get_baseline 对标保护策略目标 / save_drill_schedule）；修复 RPO 时区 bug（naive utcnow vs 本地时区 → 用 datetime.now(timezone.utc).astimezone()）；`core/data_mining.py` DataMiner（export_anonymized 脱敏导出，mask/hash/drop/fake/none 规则，PII 列自动遮蔽，DEMO 仿真 CSV）；`anonymized_exports` 表；`api/datamining.py` + `api/drills.py` 增强（trend/baseline/schedule 端点）；`templates/datamining.html` + drills.html 趋势图（自绘 SVG）/基线/排程表单；scheduler 注册 drill_schedule 周期 job。
- **QA 二线 `tests/qa_phase_3_4.py`**：Round1 38/40（2 个测试笔误），Round2 **40/40 全绿**；子进程重跑 `qa_phase_0_1_2.py` 仍 **44/44**；源码 0 缺陷（路由 NoOne）。

### 用户概念澄清（已当面解释）
- **迁移保护 vs 数据同步**：不重复但场景不同——同步=主从持续复制；迁移保护=信创迁移一次性事件的"保险"（黄金回退点+高频增量+重心切换），无迁移刚需可暂缓。
- **克隆服务**：开发/测试从生产备份自助拉起副本，用完自动销毁；测试环境多时有价值。
- **保护策略**：给核心/重要/一般系统定不同 RPO/RTO SLA，绑定任务后调度器按等级自动决定频率/并行度/存储级。现在就能用（建 general 策略绑 mysql 任务即可见效）。

### 最终运行
- 全 8 个页面 protection/migration/clone/storage/dr-link/alert/drills/datamining 均 200，端口 8080 单进程（系统 Python 3.14.3 + DEMO_MODE=on），admin/admin123。
